# ============================================
# Daily Digest Agent - 环境变量配置示例
# ============================================
# ⚠️ 重要提示�?
# 1. 复制此文件为 .env：cp env.example .env �?cp env.example backend/.env
# 2. 编辑 .env 文件，填入实际的配置值（所�?your-xxx-here 都需要替换）
# 3. 不要�?.env 文件提交到Git仓库（已�?gitignore中排除）
# 4. 此文件中的所有值都是占位符，请勿直接使用！
# ============================================

# ============================================
# 服务器配置（可选，默认值已足够�?
# ============================================
# BACKEND_PORT=6666
# FRONTEND_PORT=16666
# POSTGRES_PORT=5432

# ============================================
# 后端必需配置
# ============================================

# JWT密钥（必须修改为强随机字符串，不要使用默认值！�?
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"
# 或：openssl rand -hex 32
SECRET_KEY=your-strong-random-secret-key-change-this-in-production-please-replace

# ============================================
# LLM配置（三选一，推荐使用云端服务）
# ============================================

# 选项1：阿里云DashScope（推荐，云端服务，无需GPU�?
# 获取API Key：https://dashscope.console.aliyun.com/
LLM_PROVIDER=dashscope
DASHSCOPE_API_KEY=your-dashscope-api-key-here-replace-with-real-key

# 选项2：NVIDIA GLM API（云端服务，无需GPU�?
# LLM_PROVIDER=nvidia
# NVIDIA_API_KEY=your-nvidia-api-key-here-replace-with-real-key
# NVIDIA_MODEL=z-ai/glm4.7

# 选项3：本地Ollama（需要额外部署Ollama服务，需要GPU，不推荐�?
# LLM_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen3:8b

# ============================================
# 数据库配置（二选一�?
# ============================================

# 选项1：SQLite（默认，适合单机部署�?
DATABASE_URL=sqlite:///./daily_digest.db

# 选项2：PostgreSQL（推荐生产环境，需要docker-compose中的db服务�?
# DATABASE_URL=postgresql://postgres:postgres@db:5432/dailydigest
# POSTGRES_DB=dailydigest
# POSTGRES_USER=postgres-replace-with-real-username
# POSTGRES_PASSWORD=postgres-replace-with-strong-password

# ============================================
# CORS配置（必须配置前端域名）
# ============================================
# 多个域名用逗号分隔，不要有空格
CORS_ORIGINS=http://localhost:5173,http://localhost,http://your-domain.com

# ============================================
# 新闻API配置（至少配置一个）
# ============================================
# GNews：https://gnews.io/register（免�?00�?月）
GNEWS_API_KEY=your-gnews-api-key-here-replace-with-real-key

# NewsData.io：https://newsdata.io/register（免�?00�?天）
NEWSDATA_API_KEY=your-newsdata-api-key-here-replace-with-real-key

# ============================================
# 邮件服务配置（可选，二选一�?
# ============================================

# 选项1：Resend（推荐，免费3000�?月）
# 获取API Key：https://resend.com/
RESEND_API_KEY=your-resend-api-key-here-replace-with-real-key
FROM_EMAIL=noreply@yourdomain.com-replace-with-real-email

# 选项2：SMTP（Gmail/QQ邮箱等）
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com-replace-with-real-email
# SMTP_PASSWORD=your-app-password-here-replace-with-real-password
# DEFAULT_EMAIL_ACCOUNT=your-email@gmail.com-replace-with-real-email
# DEFAULT_EMAIL_PASSWORD=your-app-password-here-replace-with-real-password

# ============================================
# 前端配置（构建时使用�?
# ============================================
# 后端API地址（前端构建时使用，生产环境改为实际后端地址�?
VITE_API_URL=http://localhost:6666
# 生产环境示例�?
# VITE_API_URL=https://api.your-domain.com

# ============================================
# 时区配置（可选）
# ============================================
TIMEZONE=Asia/Shanghai
